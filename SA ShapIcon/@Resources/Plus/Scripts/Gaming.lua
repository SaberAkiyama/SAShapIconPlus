--; ============================================================
--; Lua Color (Gaming)
--; ============================================================

colorSelect = {
	["Mask"] = {
		colorCode = "ExecuteBatch 1"
	},
	["Gradient 1"] = {
		colorCode = "ExecuteBatch 2"
	},
	["Gradient 2"] = {
		colorCode = "ExecuteBatch 3"
	}
}

function setAmongUsColorManual(selectedCode)
	SKIN:Bang('!CommandMeasure "MeterSkinAmongUs" "' .. colorSelect[selectedCode]['colorCode'] ..  '"')

end

--; ============================================================

function setApexColorManual(selectedCode)
	SKIN:Bang('!CommandMeasure "MeterSkinApex" "' .. colorSelect[selectedCode]['colorCode'] ..  '"')

end

--; ============================================================

function setArkColorManual(selectedCode)
	SKIN:Bang('!CommandMeasure "MeterSkinArk" "' .. colorSelect[selectedCode]['colorCode'] ..  '"')

end

--; ============================================================

function setAssettoColorManual(selectedCode)
	SKIN:Bang('!CommandMeasure "MeterSkinAssetto" "' .. colorSelect[selectedCode]['colorCode'] ..  '"')

end

--; ============================================================

function setBadlionColorManual(selectedCode)
	SKIN:Bang('!CommandMeasure "MeterSkinBadlion" "' .. colorSelect[selectedCode]['colorCode'] ..  '"')

end

--; ============================================================

function setBattleNetColorManual(selectedCode)
	SKIN:Bang('!CommandMeasure "MeterSkinBattleNet" "' .. colorSelect[selectedCode]['colorCode'] ..  '"')

end

--; ============================================================

function setBF1ColorManual(selectedCode)
	SKIN:Bang('!CommandMeasure "MeterSkinBF1" "' .. colorSelect[selectedCode]['colorCode'] ..  '"')

end

--; ============================================================

function setBF4ColorManual(selectedCode)
	SKIN:Bang('!CommandMeasure "MeterSkinBF4" "' .. colorSelect[selectedCode]['colorCode'] ..  '"')

end

--; ============================================================

function setBF2042ColorManual(selectedCode)
	SKIN:Bang('!CommandMeasure "MeterSkinBF2042" "' .. colorSelect[selectedCode]['colorCode'] ..  '"')

end

--; ============================================================

function setBFVColorManual(selectedCode)
	SKIN:Bang('!CommandMeasure "MeterSkinBFV" "' .. colorSelect[selectedCode]['colorCode'] ..  '"')

end

--; ============================================================

function setBethesdaColorManual(selectedCode)
	SKIN:Bang('!CommandMeasure "MeterSkinBethesda" "' .. colorSelect[selectedCode]['colorCode'] ..  '"')

end

--; ============================================================

function setBDOColorManual(selectedCode)
	SKIN:Bang('!CommandMeasure "MeterSkinBDO" "' .. colorSelect[selectedCode]['colorCode'] ..  '"')

end

--; ============================================================

function setBTD6ColorManual(selectedCode)
	SKIN:Bang('!CommandMeasure "MeterSkinBTD6" "' .. colorSelect[selectedCode]['colorCode'] ..  '"')

end

--; ============================================================

function setCODMWColorManual(selectedCode)
	SKIN:Bang('!CommandMeasure "MeterSkinCODMW" "' .. colorSelect[selectedCode]['colorCode'] ..  '"')

end

--; ============================================================

function setCODVanguardColorManual(selectedCode)
	SKIN:Bang('!CommandMeasure "MeterSkinCODVanguard" "' .. colorSelect[selectedCode]['colorCode'] ..  '"')

end

--; ============================================================

function setCloneHeroColorManual(selectedCode)
	SKIN:Bang('!CommandMeasure "MeterSkinCloneHero" "' .. colorSelect[selectedCode]['colorCode'] ..  '"')

end

--; ============================================================

function setCSGOColorManual(selectedCode)
	SKIN:Bang('!CommandMeasure "MeterSkinCSGO" "' .. colorSelect[selectedCode]['colorCode'] ..  '"')

end

--; ============================================================

function setCyberpunkColorManual(selectedCode)
	SKIN:Bang('!CommandMeasure "MeterSkinCyberpunk" "' .. colorSelect[selectedCode]['colorCode'] ..  '"')

end

--; ============================================================

function setDauntlessColorManual(selectedCode)
	SKIN:Bang('!CommandMeasure "MeterSkinDauntless" "' .. colorSelect[selectedCode]['colorCode'] ..  '"')

end

--; ============================================================

function setDBDColorManual(selectedCode)
	SKIN:Bang('!CommandMeasure "MeterSkinDBD" "' .. colorSelect[selectedCode]['colorCode'] ..  '"')

end

--; ============================================================

function setDestiny2ColorManual(selectedCode)
	SKIN:Bang('!CommandMeasure "MeterSkinDestiny2" "' .. colorSelect[selectedCode]['colorCode'] ..  '"')

end

--; ============================================================

function setDOOMColorManual(selectedCode)
	SKIN:Bang('!CommandMeasure "MeterSkinDOOM" "' .. colorSelect[selectedCode]['colorCode'] ..  '"')

end

--; ============================================================

function setDOOMEternalColorManual(selectedCode)
	SKIN:Bang('!CommandMeasure "MeterSkinDOOMEternal" "' .. colorSelect[selectedCode]['colorCode'] ..  '"')

end

--; ============================================================

function setDotaColorManual(selectedCode)
	SKIN:Bang('!CommandMeasure "MeterSkinDota" "' .. colorSelect[selectedCode]['colorCode'] ..  '"')

end

--; ============================================================

function setEFTColorManual(selectedCode)
	SKIN:Bang('!CommandMeasure "MeterSkinEFT" "' .. colorSelect[selectedCode]['colorCode'] ..  '"')

end

--; ============================================================

function setEGStoreColorManual(selectedCode)
	SKIN:Bang('!CommandMeasure "MeterSkinEGStore" "' .. colorSelect[selectedCode]['colorCode'] ..  '"')

end

--; ============================================================

function setESOColorManual(selectedCode)
	SKIN:Bang('!CommandMeasure "MeterSkinESO" "' .. colorSelect[selectedCode]['colorCode'] ..  '"')

end

--; ============================================================

function setESVSkyrimColorManual(selectedCode)
	SKIN:Bang('!CommandMeasure "MeterSkinESVSkyrim" "' .. colorSelect[selectedCode]['colorCode'] ..  '"')

end

--; ============================================================

function setFactorioColorManual(selectedCode)
	SKIN:Bang('!CommandMeasure "MeterSkinFactorio" "' .. colorSelect[selectedCode]['colorCode'] ..  '"')

end

--; ============================================================

function setFallGuysColorManual(selectedCode)
	SKIN:Bang('!CommandMeasure "MeterSkinFallGuys" "' .. colorSelect[selectedCode]['colorCode'] ..  '"')

end

--; ============================================================

function setFFXIVColorManual(selectedCode)
	SKIN:Bang('!CommandMeasure "MeterSkinFFXIV" "' .. colorSelect[selectedCode]['colorCode'] ..  '"')

end

--; ============================================================

function setFNFColorManual(selectedCode)
	SKIN:Bang('!CommandMeasure "MeterSkinFNF" "' .. colorSelect[selectedCode]['colorCode'] ..  '"')

end

--; ============================================================

function setFortniteColorManual(selectedCode)
	SKIN:Bang('!CommandMeasure "MeterSkinFortnite" "' .. colorSelect[selectedCode]['colorCode'] ..  '"')

end

--; ============================================================

function setFH4ColorManual(selectedCode)
	SKIN:Bang('!CommandMeasure "MeterSkinFH4" "' .. colorSelect[selectedCode]['colorCode'] ..  '"')

end

--; ============================================================

function setFH5ColorManual(selectedCode)
	SKIN:Bang('!CommandMeasure "MeterSkinFH5" "' .. colorSelect[selectedCode]['colorCode'] ..  '"')

end

--; ============================================================

function setGameLibrariesColorManual(selectedCode)
	SKIN:Bang('!CommandMeasure "MeterSkinGameLibraries" "' .. colorSelect[selectedCode]['colorCode'] ..  '"')

end

--; ============================================================

function setGameHUB2ColorManual(selectedCode)
	SKIN:Bang('!CommandMeasure "MeterSkinGameHUB2" "' .. colorSelect[selectedCode]['colorCode'] ..  '"')

end

--; ============================================================

function setGModColorManual(selectedCode)
	SKIN:Bang('!CommandMeasure "MeterSkinGMod" "' .. colorSelect[selectedCode]['colorCode'] ..  '"')

end

--; ============================================================

function setGenshinColorManual(selectedCode)
	SKIN:Bang('!CommandMeasure "MeterSkinGenshin" "' .. colorSelect[selectedCode]['colorCode'] ..  '"')

end

--; ============================================================

function setGhostrunnerColorManual(selectedCode)
	SKIN:Bang('!CommandMeasure "MeterSkinGhostrunner" "' .. colorSelect[selectedCode]['colorCode'] ..  '"')

end

--; ============================================================

function setGodfallColorManual(selectedCode)
	SKIN:Bang('!CommandMeasure "MeterSkinGodfall" "' .. colorSelect[selectedCode]['colorCode'] ..  '"')

end

--; ============================================================

function setGOGColorManual(selectedCode)
	SKIN:Bang('!CommandMeasure "MeterSkinGOG" "' .. colorSelect[selectedCode]['colorCode'] ..  '"')

end

--; ============================================================

function setGTAVColorManual(selectedCode)
	SKIN:Bang('!CommandMeasure "MeterSkinGTAV" "' .. colorSelect[selectedCode]['colorCode'] ..  '"')

end

--; ============================================================

function setGW2ColorManual(selectedCode)
	SKIN:Bang('!CommandMeasure "MeterSkinGW2" "' .. colorSelect[selectedCode]['colorCode'] ..  '"')

end

--; ============================================================

function setGuiltyGearColorManual(selectedCode)
	SKIN:Bang('!CommandMeasure "MeterSkinGuiltyGear" "' .. colorSelect[selectedCode]['colorCode'] ..  '"')

end

--; ============================================================

function setHaloInfiniteColorManual(selectedCode)
	SKIN:Bang('!CommandMeasure "MeterSkinHaloInfinite" "' .. colorSelect[selectedCode]['colorCode'] ..  '"')

end

--; ============================================================

function setHearthstoneColorManual(selectedCode)
	SKIN:Bang('!CommandMeasure "MeterSkinHearthstone" "' .. colorSelect[selectedCode]['colorCode'] ..  '"')

end

--; ============================================================

function setHOI4ColorManual(selectedCode)
	SKIN:Bang('!CommandMeasure "MeterSkinHOI4" "' .. colorSelect[selectedCode]['colorCode'] ..  '"')

end

--; ============================================================

function setHOTSColorManual(selectedCode)
	SKIN:Bang('!CommandMeasure "MeterSkinHOTS" "' .. colorSelect[selectedCode]['colorCode'] ..  '"')

end

--; ============================================================

function setHonkaiColorManual(selectedCode)
	SKIN:Bang('!CommandMeasure "MeterSkinHonkai" "' .. colorSelect[selectedCode]['colorCode'] ..  '"')

end

--; ============================================================

function setHuntShowdownColorManual(selectedCode)
	SKIN:Bang('!CommandMeasure "MeterSkinHuntShowdown" "' .. colorSelect[selectedCode]['colorCode'] ..  '"')

end

--; ============================================================

function setLOLColorManual(selectedCode)
	SKIN:Bang('!CommandMeasure "MeterSkinLOL" "' .. colorSelect[selectedCode]['colorCode'] ..  '"')

end

--; ============================================================

function setLORColorManual(selectedCode)
	SKIN:Bang('!CommandMeasure "MeterSkinLOR" "' .. colorSelect[selectedCode]['colorCode'] ..  '"')

end

--; ============================================================

function setLunarColorManual(selectedCode)
	SKIN:Bang('!CommandMeasure "MeterSkinLunar" "' .. colorSelect[selectedCode]['colorCode'] ..  '"')

end

--; ============================================================

function setMinecraftColorManual(selectedCode)
	SKIN:Bang('!CommandMeasure "MeterSkinMinecraft" "' .. colorSelect[selectedCode]['colorCode'] ..  '"')

end

--; ============================================================

function setMHWorldColorManual(selectedCode)
	SKIN:Bang('!CommandMeasure "MeterSkinMHWorld" "' .. colorSelect[selectedCode]['colorCode'] ..  '"')

end

--; ============================================================

function setNarakaColorManual(selectedCode)
	SKIN:Bang('!CommandMeasure "MeterSkinNaraka" "' .. colorSelect[selectedCode]['colorCode'] ..  '"')

end

--; ============================================================

function setNierAutomataColorManual(selectedCode)
	SKIN:Bang('!CommandMeasure "MeterSkinNierAutomata" "' .. colorSelect[selectedCode]['colorCode'] ..  '"')

end

--; ============================================================

function setNMSColorManual(selectedCode)
	SKIN:Bang('!CommandMeasure "MeterSkinNMS" "' .. colorSelect[selectedCode]['colorCode'] ..  '"')

end

--; ============================================================

function setOriginColorManual(selectedCode)
	SKIN:Bang('!CommandMeasure "MeterSkinOrigin" "' .. colorSelect[selectedCode]['colorCode'] ..  '"')

end

--; ============================================================

function setOriBFColorManual(selectedCode)
	SKIN:Bang('!CommandMeasure "MeterSkinOriBF" "' .. colorSelect[selectedCode]['colorCode'] ..  '"')

end

--; ============================================================

function setOriWWColorManual(selectedCode)
	SKIN:Bang('!CommandMeasure "MeterSkinOriWW" "' .. colorSelect[selectedCode]['colorCode'] ..  '"')

end

--; ============================================================

function setOsuColorManual(selectedCode)
	SKIN:Bang('!CommandMeasure "MeterSkinOsu" "' .. colorSelect[selectedCode]['colorCode'] ..  '"')

end

--; ============================================================

function setOutridersColorManual(selectedCode)
	SKIN:Bang('!CommandMeasure "MeterSkinOutriders" "' .. colorSelect[selectedCode]['colorCode'] ..  '"')

end

--; ============================================================

function setOverwatchColorManual(selectedCode)
	SKIN:Bang('!CommandMeasure "MeterSkinOverwatch" "' .. colorSelect[selectedCode]['colorCode'] ..  '"')

end

--; ============================================================

function setPaladinsColorManual(selectedCode)
	SKIN:Bang('!CommandMeasure "MeterSkinPaladins" "' .. colorSelect[selectedCode]['colorCode'] ..  '"')

end

--; ============================================================

function setPOEColorManual(selectedCode)
	SKIN:Bang('!CommandMeasure "MeterSkinPOE" "' .. colorSelect[selectedCode]['colorCode'] ..  '"')

end

--; ============================================================

function setPUBGColorManual(selectedCode)
	SKIN:Bang('!CommandMeasure "MeterSkinPUBG" "' .. colorSelect[selectedCode]['colorCode'] ..  '"')

end

--; ============================================================

function setRSSiegeColorManual(selectedCode)
	SKIN:Bang('!CommandMeasure "MeterSkinRSSiege" "' .. colorSelect[selectedCode]['colorCode'] ..  '"')

end

--; ============================================================

function setRecRoomColorManual(selectedCode)
	SKIN:Bang('!CommandMeasure "MeterSkinRecRoom" "' .. colorSelect[selectedCode]['colorCode'] ..  '"')

end

--; ============================================================

function setRiotColorManual(selectedCode)
	SKIN:Bang('!CommandMeasure "MeterSkinRiot" "' .. colorSelect[selectedCode]['colorCode'] ..  '"')

end

--; ============================================================

function setRobloxColorManual(selectedCode)
	SKIN:Bang('!CommandMeasure "MeterSkinRoblox" "' .. colorSelect[selectedCode]['colorCode'] ..  '"')

end

--; ============================================================

function setRobloxStudioColorManual(selectedCode)
	SKIN:Bang('!CommandMeasure "MeterSkinRobloxStudio" "' .. colorSelect[selectedCode]['colorCode'] ..  '"')

end

--; ============================================================

function setRocketLeagueColorManual(selectedCode)
	SKIN:Bang('!CommandMeasure "MeterSkinRocketLeague" "' .. colorSelect[selectedCode]['colorCode'] ..  '"')

end

--; ============================================================

function setRockstarColorManual(selectedCode)
	SKIN:Bang('!CommandMeasure "MeterSkinRockstar" "' .. colorSelect[selectedCode]['colorCode'] ..  '"')

end

--; ============================================================

function setRSIColorManual(selectedCode)
	SKIN:Bang('!CommandMeasure "MeterSkinRSI" "' .. colorSelect[selectedCode]['colorCode'] ..  '"')

end

--; ============================================================

function setRuneScapeColorManual(selectedCode)
	SKIN:Bang('!CommandMeasure "MeterSkinRuneScape" "' .. colorSelect[selectedCode]['colorCode'] ..  '"')

end

--; ============================================================

function setRustColorManual(selectedCode)
	SKIN:Bang('!CommandMeasure "MeterSkinRust" "' .. colorSelect[selectedCode]['colorCode'] ..  '"')

end

--; ============================================================

function setSatisfactoryColorManual(selectedCode)
	SKIN:Bang('!CommandMeasure "MeterSkinSatisfactory" "' .. colorSelect[selectedCode]['colorCode'] ..  '"')

end

--; ============================================================

function setSoTColorManual(selectedCode)
	SKIN:Bang('!CommandMeasure "MeterSkinSoT" "' .. colorSelect[selectedCode]['colorCode'] ..  '"')

end

--; ============================================================

function setStSColorManual(selectedCode)
	SKIN:Bang('!CommandMeasure "MeterSkinStS" "' .. colorSelect[selectedCode]['colorCode'] ..  '"')

end

--; ============================================================

function setSmiteColorManual(selectedCode)
	SKIN:Bang('!CommandMeasure "MeterSkinSmite" "' .. colorSelect[selectedCode]['colorCode'] ..  '"')

end

--; ============================================================

function setStarCitizenColorManual(selectedCode)
	SKIN:Bang('!CommandMeasure "MeterSkinStarCitizen" "' .. colorSelect[selectedCode]['colorCode'] ..  '"')

end

--; ============================================================

function setSteamColorManual(selectedCode)
	SKIN:Bang('!CommandMeasure "MeterSkinSteam" "' .. colorSelect[selectedCode]['colorCode'] ..  '"')

end

--; ============================================================

function setSAPColorManual(selectedCode)
	SKIN:Bang('!CommandMeasure "MeterSkinSAP" "' .. colorSelect[selectedCode]['colorCode'] ..  '"')

end

--; ============================================================

function setTF2ColorManual(selectedCode)
	SKIN:Bang('!CommandMeasure "MeterSkinTF2" "' .. colorSelect[selectedCode]['colorCode'] ..  '"')

end

--; ============================================================

function setTW3ColorManual(selectedCode)
	SKIN:Bang('!CommandMeasure "MeterSkinTW3" "' .. colorSelect[selectedCode]['colorCode'] ..  '"')

end

--; ============================================================

function setUbisoftColorManual(selectedCode)
	SKIN:Bang('!CommandMeasure "MeterSkinUbisoft" "' .. colorSelect[selectedCode]['colorCode'] ..  '"')

end

--; ============================================================

function setValorantColorManual(selectedCode)
	SKIN:Bang('!CommandMeasure "MeterSkinValorant" "' .. colorSelect[selectedCode]['colorCode'] ..  '"')

end

--; ============================================================

function setVortexColorManual(selectedCode)
	SKIN:Bang('!CommandMeasure "MeterSkinVortex" "' .. colorSelect[selectedCode]['colorCode'] ..  '"')

end

--; ============================================================

function setWarThunderColorManual(selectedCode)
	SKIN:Bang('!CommandMeasure "MeterSkinWarThunder" "' .. colorSelect[selectedCode]['colorCode'] ..  '"')

end

--; ============================================================

function setWarframeColorManual(selectedCode)
	SKIN:Bang('!CommandMeasure "MeterSkinWarframe" "' .. colorSelect[selectedCode]['colorCode'] ..  '"')

end

--; ============================================================

function setWOTanksColorManual(selectedCode)
	SKIN:Bang('!CommandMeasure "MeterSkinWOTanks" "' .. colorSelect[selectedCode]['colorCode'] ..  '"')

end

--; ============================================================

function setWOWColorManual(selectedCode)
	SKIN:Bang('!CommandMeasure "MeterSkinWOW" "' .. colorSelect[selectedCode]['colorCode'] ..  '"')

end

--; ============================================================

function setWOWarshipsColorManual(selectedCode)
	SKIN:Bang('!CommandMeasure "MeterSkinWOWarships" "' .. colorSelect[selectedCode]['colorCode'] ..  '"')

end

--; ============================================================

function setXboxColorManual(selectedCode)
	SKIN:Bang('!CommandMeasure "MeterSkinXbox" "' .. colorSelect[selectedCode]['colorCode'] ..  '"')

end

--; ============================================================
--; ============================================================

hoverCategorySelect = {
	["Over"] = {
		colorApply = "255,215,0"
	},
	["Leave"] = {
		colorApply = "255,255,255"
	}
}

function setSkinApply(selectedHover)
	SKIN:Bang('!SetOption MeterApplyButton FontColor "' .. hoverCategorySelect[selectedHover]['colorApply'] .. '"')
	SKIN:Bang('!UpdateMeter *')
	SKIN:Bang('!Redraw')

end

function setApply()
	SKIN:Bang('!RefreshGroup SIGaming')

end
